<div class="brand-moment" aria-labelledby="brand-moment-title">
  <span class="brand-moment__shape brand-moment__shape--small" aria-hidden="true"></span>
  <span class="brand-moment__spark" aria-hidden="true">✦</span>

  <div class="brand-moment__content container">
    <div class="brand-moment__text">
      <h2 class="brand-moment__title" id="brand-moment-title">
        Pequeños antojos,<br />grandes momentos.
      </h2>
      <p class="brand-moment__supporting">Hechos para compartir. ¿O no?</p>
    </div>

    <div class="brand-moment__media">
      <img
        class="brand-moment__image"
        src="/images/stock/mix_1.jpg"
        alt="Selección de galletas artesanales sobre una bandeja"
        width="3024"
        height="4032"
        loading="lazy"
        decoding="async"
      />
    </div>
  </div>
</div>

<style>
  .brand-moment {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    color: var(--color-ink);
    background: var(--color-primary);
  }

  .brand-moment__content {
    position: relative;
    z-index: 1;
    display: grid;
    gap: var(--space-5);
    padding-block: clamp(var(--space-6), 1.5rem + 2vw, 3.5rem);
  }

  .brand-moment__text {
    display: grid;
    align-content: center;
    gap: clamp(1rem, 0.8rem + 0.8vw, 1.5rem);
  }

  .brand-moment__title {
    max-width: 16ch;
    font-size: clamp(2rem, 7vw, 3.5rem);
    font-weight: var(--font-weight-bold);
    line-height: 1.08;
  }

  .brand-moment__supporting {
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-medium);
  }

  .brand-moment__media {
    width: 100%;
    overflow: hidden;
    border-radius: var(--radius-lg);
  }

  .brand-moment__image {
    width: 100%;
    aspect-ratio: 16 / 9;
    height: 100%;
    object-fit: cover;
    object-position: center 62%;
  }

  .brand-moment__shape,
  .brand-moment__spark {
    position: absolute;
    z-index: 0;
    pointer-events: none;
  }

  .brand-moment__shape {
    aspect-ratio: 1;
    border-radius: 50%;
  }

  .brand-moment__shape--small {
    width: min(18vw, 5rem);
    top: var(--space-4);
    right: 6%;
    background: var(--color-highlight);
  }

  .brand-moment__spark {
    top: 14%;
    left: 5%;
    color: var(--color-accent-light);
    font-size: var(--font-size-xl);
  }

  @media (min-width: 48rem) {
    .brand-moment__content {
      grid-template-columns: minmax(0, 1.15fr) minmax(18rem, 0.85fr);
      align-items: center;
      gap: clamp(var(--space-6), 5vw, var(--space-8));
    }

    .brand-moment__shape--small {
      top: -8%;
      right: 46%;
    }
  }
</style>
