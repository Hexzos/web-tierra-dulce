import{adminApi}from'./admin-api-client.js';const root=document.querySelector('[data-revisions]');if(root){let status='pending',timer;const list=root.querySelector('[data-revision-list]'),search=root.querySelector('[data-revision-search]'),message=root.querySelector('[data-revision-message]');const load=async()=>{try{const rows=await adminApi(`/api/admin/revisions?status=${status}&search=${encodeURIComponent(search.value)}`);list.replaceChildren(...rows.map(r=>{const row=document.createElement('article');row.className='admin-product-row';const info=document.createElement('div'),title=document.createElement('strong'),meta=document.createElement('p'),a=document.createElement('a');title.textContent=r.productName;meta.textContent=`${r.submitterName??'Perfil eliminado'} · ${new Date(r.createdAt).toLocaleString('es-CL')} · versión base ${r.baseVersion} · ${r.status}`;a.href=`/admin/revisiones/ver/?id=${r.id}`;a.textContent='Revisar';a.className='admin-secondary-action';info.append(title,meta);row.append(info,a);return row}));message.textContent=`${rows.length} revisiones`}catch(e){message.textContent=e.message}};root.querySelectorAll('[data-revision-status]').forEach(b=>b.addEventListener('click',()=>{status=b.dataset.revisionStatus;root.querySelectorAll('[data-revision-status]').forEach(x=>x.setAttribute('aria-selected',String(x===b)));load()}));search.addEventListener('input',()=>{clearTimeout(timer);timer=setTimeout(load,250)});load()}
